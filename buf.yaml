build:
  roots:
    - protobuf
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX # re-enable after stabilizing API
  ignore_only:
    ENUM_ZERO_VALUE_SUFFIX:
      - protocol/enums.proto
